CREATE TABLE users(id BIGSERIAL NOT NULL PRIMARY KEY,first_name VARCHAR(250) NOT NULL ,last_name VARCHAR(250) NOT NULL,email TEXT NOT NULL ,password VARCHAR NOT NULL,created_at TIMESTAMP WITH TIME ZONE ,UNIQUE(email));